addons:
  postgresql: 9.6
  code_climate:
      repo_token: 2e4e7db53880ddf074bfa6dbb42884e29df445ec5ff29fe709b6468ceb36dd34
after_success:
  - bundle install
  - bundle exec codeclimate-test-reporter
before_script:
  - cp config/database.yml.travis config/database.yml
  - psql -c 'create database travis_ci_test;' -U postgres
dist: trusty
language: ruby
rvm:
  - 2.7.0
script:
  - bundle exec rails db:migrate RAILS_ENV=test
  - bundle exec rspec
